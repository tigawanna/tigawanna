# Landing e2e moved

Playwright coverage now lives at `apps/portfolio/e2e/landing/`.

E2E is coupled to the **app** (server, QueryClient, routes), not the shared
`@repo/ui/landing` package. See that folder’s README.
